# ProcessMonster Banking BPM - Environment Configuration
# Copy this file to .env and update with your values

# ============================================
# Database Configuration
# ============================================
POSTGRES_DB=processmonster
POSTGRES_USER=processmonster_user
POSTGRES_PASSWORD=changeme_strong_password_here
POSTGRES_PORT=5432

# ============================================
# Backend Configuration
# ============================================
BACKEND_PORT=8080
SPRING_PROFILES_ACTIVE=prod
SPRING_JPA_DDL_AUTO=update
SPRING_JPA_SHOW_SQL=false

# JWT Configuration
JWT_SECRET=your-very-secure-secret-key-minimum-256-bits-change-this-in-production
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:4200,http://localhost:80

# File Upload Configuration
UPLOAD_MAX_FILE_SIZE=10MB
UPLOAD_PATH=/app/uploads

# Logging Configuration
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_APP=INFO

# ============================================
# Frontend Configuration
# ============================================
FRONTEND_PORT=80

# ============================================
# pgAdmin Configuration (Optional - Dev only)
# ============================================
PGADMIN_EMAIL=admin@processmonster.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ============================================
# Production Deployment
# ============================================
# For production, override these values:
# - Use strong passwords (minimum 32 characters)
# - Use a secure JWT_SECRET (minimum 256 bits)
# - Set SPRING_JPA_DDL_AUTO=validate (never use update in prod)
# - Set SPRING_JPA_SHOW_SQL=false
# - Configure proper CORS_ALLOWED_ORIGINS
# - Use HTTPS for all connections
# - Enable proper logging and monitoring
